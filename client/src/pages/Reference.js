import React from 'react';
import { Typography } from '@mui/material';

const Reference = () => {
	return (
		<React.Fragment>
			<Typography variant='h1'>h1. HEADING.</Typography>
			<Typography variant='h2'>h2. Sub-Heading.</Typography>
			<Typography variant='body1'>body1. Body text.</Typography>
			<Typography variant='current'>current. CURRENT MEMBERS</Typography>
			<Typography variant='past'>past. PAST MEMBERS</Typography>
			<Typography variant='characters'>幻道太鼓</Typography>
		</React.Fragment>
	);
};

export default Reference;